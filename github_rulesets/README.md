# GitHub Rulesets

Rulesets for this org.

Single source of truth for repo rulesets.

Use these JSON files to import rules into repositories. This folder is just a simple storage for various rulesets for GitHub repo configuration.
